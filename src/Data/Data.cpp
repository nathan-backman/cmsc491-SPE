// Copyright 2019 [BVU CMSC491 class]
#include "Data/Data.h"

/*
template<typename T>
Data<T>::Data(T value) {
  this->value = value;
  gettimeofday(&timestamp, NULL);  // Gets the current time
}

template<typename T>
Data<T>::Data(T value, timeval timestamp) {
  this->value = value;
  this->timestamp = timestamp;  // Applies the provided timestamp
}
*/
